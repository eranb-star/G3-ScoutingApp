# Decision support, recovery and engineering acceptance — 24 September 2026

User authorized all three increments, beginning with recovery/permissions. Extend existing features and deploy independently verified increments. Preserve budget, roles, simulator and private user files. Do not claim complete recovery while offsite/key custody or application/auth recovery is unverified.

## Live baseline

- Scheduled local Storage backup last succeeded September 21, missed two runs. Manual September 24 backup captured 12 files / 10,176,996 bytes, encrypted and verified; restored to a new ignored local directory with exact checksums. This is local same-profile recovery, not offsite.
- Destination and separate portable recovery-key custody requested from owner; awaiting answer. No external backup destination invented.
- Production read-only audit: no exposed public tables lacking RLS; two unpinned-search-path legacy import routines. Anonymous EXECUTE exposed unguarded event imports, sync_team_action and close_stale_workshop_sessions. Tightening migration passed local tests and hosted QA and is applied to production. Anonymous/authenticated direct EXECUTE on the four unsafe helpers is now absent. Scheduled jobs continued succeeding. Three legacy role guards additionally changed to reject NULL roles (inactive/missing membership); existing supervisor roles retained.

## Increment acceptance

1. Recovery/permissions: fresh backup and restoration; isolated nonempty cloud-object/access rehearsal; live catalog/role audit and fixes; portable/offsite recovery only to owner-approved destination; external configuration and measured recovery evidence. Keep recovery clone schedulers disabled.
2. Decision support: focused intent/question flow, evidence/assumptions/options/test output, contextual follow-ups, readable EN/HE presentation, saved answer-to-task provenance; representative repeatable evaluations. No second chatbot, corpus reimport or task system.
3. Existing workflow: owner/dependencies, named mentor, changes requested/resubmission, stale requirements, unauthorized/self approvals, duplicate retry and offline draft behavior. Use synthetic QA; preserve production work. Physical phone/workshop checks remain explicit pending items.

## Work discovered

Create task currently transfers only a title, losing the answer, citations and proposed test. Existing Software Mentor review handoff is code-only. Extend these flows using server-verified saved answer provenance and current task/review permissions, with explicit share-to-project preview and retry identity.

## Implemented and checked

- Production migrations: `permission_baseline_20260924.sql`, `permission_null_roles_20260924.sql`, and additive `assist_task_context_20260924.sql`. No role widening, budget change or simulator change.
- Assistant backend deployed and persisted source compared exactly: SHA256 `0d509725c2f0f7f5fa2242f2a18c40ff70b71b1353959cc58a8b99fe5a97d6b2`. Adds practical decision structure and bounded user-question context for short follow-up retrieval. Does not use earlier assistant claims as evidence. Explicit new seasons override prior context.
- Saved answer handoff uses owned message UUID, existing create-task permissions, an atomic immutable question/answer/citation/software-revision snapshot and a per-member retry identity. Saved snapshots remain if the private source conversation is deleted. Explicit UI notice explains sharing to task readers. No automatic mentor approval or passing result.
- Answer headings, tables, lists and code render as safe React text, with expandable source links, EN/HE presentation and larger mobile actions. No raw model HTML execution.
- Task list fetches only embedded snapshot IDs; full answers load when the user opens the context. No per-task background full-answer queries.
- Local tests passed: permission baseline/NULL role denial; actual engineering records/review/requirements/dependency/archive/deletion controls; assistant endpoint access; answer ownership, hidden-project/inactive visibility, source retention, duplicate retry and transaction rollback; EN/HE retrieval context and draft parsing. TypeScript and Vite production build passed (existing large chunk warning remains).
- Hosted QA actor workflow passed with all synthetic writes rolled back. Hosted answer-to-task acceptance also passed after aligning QA's missing existing collaborator routines with production. Production already had these routines; no new collaborator workflow was introduced.
- Browser: actual assistant/project components verified with synthetic answers; interrupted save retains draft; retry creates one task; question, answer, sources and To do state displayed. Hebrew 390px viewport fits without horizontal overflow. This is responsive browser QA, not physical phone acceptance.
- Website deployed: source `8fb99dc43fa069a7b6878b3ae3c00b70ee5ca53c`, Vercel `NefhtgnnVSdxYfQDPDRTYc3XbaRm`, Ready production at https://g3-6740.com. Production-environment rebuild used; production UI verified. Subsequent changes are backend/QA/documentation only.

## Release verification and lessons

- Final assistant bundle SHA256 `643b49d7504cf3d3037eb9c05532c82e8c37e5e664bf8f374be955fa4619ca54`; persisted production editor compared to built source, equal after line-ending normalization. This supersedes the earlier bundle hash above.
- Live evaluation caught a real retrieval defect: a long climb strategy question returned generic overview text but omitted the scoring table. Fixed bounded query term selection and added `official_scoring_retrieval_20260924.sql` (QA and production applied successfully). Actual production retrieval now starts with manual page 47, keeps pages 46/48, and contains TELEOP 10/20/30, AUTO Level 1 15 and TRAVERSAL threshold 50. Selection preserves current indexed fingerprints, season/access boundaries and the eight-passage maximum. No game values or page IDs hardcoded in the retrieval fix. Regression fixture includes distracting overview pages and table continuation; complete ingestion suite passed.
- An intermediate paid answer then cited correct values but invented weight assumptions and proposed zero structural deflection. Further prompt correction prohibits unsupported numeric engineering assumptions, guarantees and impossible zero-deflection criteria; uses missing measurements and mentor-agreed tolerances. Do not treat earlier saved answers as corrected automatically, or claim universal answer accuracy from one test.
- Real task/review components plus actual PostgreSQL fixture passed the complete sequence: student draft reload; submit; dependent completion blocked; mentor requests changes; student resubmits a new immutable artifact; approval denied until artifact verification; mentor approves; source becomes done; dependent task can then complete. Synthetic QA only: no production task or review notification created. This does not certify a real robot or replace mentor judgment.
- Final answer-to-task retry safeguard rejects a reused request identity with changed project, source message, title, owner, due date or collaborators. Same-input retry returns the original task. Updated migration applied to QA and production; local transactional tests passed.
- Repeated Hebrew follow-up testing exposed two more defects: the answer confused sufficient versus necessary alliance combinations; and a second short follow-up lost the original season anchor. Instructions now require mixed-level arithmetic and concise factual follow-ups. Retrieval now walks up to five prior user questions (within the ten-message history window) back to the current topic anchor, stopping at a substantive new topic or explicit season. Local regressions cover the repeated-follow-up failure, assistant-text exclusion and topic/season replacement. This is bounded context, not unlimited memory. Safe formatting covers headings/lists/tables/code; arbitrary Markdown/math is not fully supported. Physical phone/workshop and broader question-set evaluation remain follow-ups.
- Final English clean-conversation evaluation: correct official 10/20/30 and AUTO 15/50-RP values and citations, missing robot measurements, explicit proposed 10-trial/4-second target, and permanent deformation distinguished from elastic deflection. No invented numeric weight saving in this final answer. The 4-second target is a proposal, not validated team performance.
- Repeated Hebrew follow-up regained the scoring evidence after the context fix, but an already error-contaminated conversation still repeated an incorrect necessary-condition claim (three Level 2 climbs, overlooking 20+20+10). Do not label this a full AI reasoning acceptance pass. Existing saved answers are not rewritten. Removed a conflicting instruction that forced every factual follow-up to include design tests/tradeoffs. Further representative evaluations and review of AI conclusions remain required; citations alone do not establish reasoning correctness.
- Seven paid production evaluations since 08:55 UTC settled for 119,027 micro-USD ($0.119027), zero unsettled attempts. The additional independent Hebrew evaluation after the final formatting-instruction correction was denied by the existing daily spending allowance before another provider attempt; checking request status cleared the pending UI state. Monthly cap remains $25 and enabled; no allowance raised. The final instruction correction is deployed/source-verified but that additional live answer is NOT validated. The displayed question-count remainder is separate from the spending allowance and can be misleading; unify those indicators in a follow-up UX correction.

## Current completion boundary / next work

1. **Delivered:** production permission tightening; encrypted local backup/restore; decision-support presentation and immutable answer-to-existing-task handoff; production scoring-table retrieval; existing engineering review/dependency acceptance in synthetic browser/database QA.
2. **Needs owner input:** independent backup destination and separately protected portable key custody. Then portable clean-machine restore and full database/auth/storage/function/environment recovery rehearsal with measured RPO/RTO. Nonempty cloud-object/access recovery is also still outstanding.
3. **Needs workshop/device evidence:** actual offseason repository selection, representative robot logs, physical phone/headset/workshop checks and real mentor acceptance. No new repo/log request while the owner is away.
4. **Next remote quality increment:** deterministic scoring-combination validation or equivalent evaluated reasoning checks; broaden English/Hebrew scenarios and repeated-follow-up tests after allowance reset; distinguish question-count and spend-limit remaining values in the UI. Do not describe decision support as fully accepted until the known mixed-level reasoning case passes. No paid retry scheduler or new spending authorization created.

## Rollback

Previous website: source `3e67735`, Vercel `GoEEZfFZGZ33xRFrAR1kUHmwbasb`. Rebuild that source with production environment if rollback is required. Keep additive task provenance tables/data and tightened security grants; do not restore the insecure helper permissions. Backend source is versioned under `backend/supabase/functions/frc-assistant`; deploy a reviewed prior bundle only after checking compatibility with retained schema. Scoring search can be restored from the prior ingestion function if required, but that restores the known scoring-table retrieval defect.

## Recovery boundaries

September 24 storage archive: `g3-storage-20260924T080242Z-0fccad.g3backup`, 12 objects / 10,176,996 source bytes. Actual restore to ignored `docs/staging/recovery-20260924.local` verified every object checksum. Existing scheduled backup last automatic success September 21 with two missed runs. Computer/profile dependence is unresolved until offsite/portable recovery is configured. The September 13 recovery clone is historical, contains real data, and is not a routine QA environment.

Still needed: approved independent destination and separate key custody; portable clean-machine restore; nonempty cloud-object access recovery; current database/auth/function/environment reconstruction and measured full-service RPO/RTO. Do not equate local file restoration with complete disaster recovery. Do not commit credentials, restored objects, private data or backup keys.
