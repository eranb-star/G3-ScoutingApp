# Authorized A–E engineering programme — 24 September 2026

User explicitly authorized implementing A through E together. This supersedes the prior awaiting-selection and simulator-deferral wording for this scope. Do not ask again whether to start. It does not authorize inventing robot measurements, raising spending caps, obtaining new private access, or actuating/deploying to the physical robot. Extend existing features and preserve provenance, human review and current production behavior.

## Required outcomes and release gates

## Continuation on 24 September — local implementation, production unchanged

This section supersedes the first-checkpoint limitations below where explicitly stated. The complete A–E programme remains authorized and unfinished; do not ask the user to authorize it again or present these increments as programme completion.

- **A:** Added `season_rule_authority_20260924.sql`: separately saved scoring revisions, explicit administrator review/activation, append-only activation history, deactivation, rollback to still-current versions, optimistic revision checks and read-time invalidation when a referenced document hash/index changes. Normal ingestion does not approve a rule. Actual PostgreSQL fixture tests cover repeat application, permissions including absent identity, stale source rejection, immutable client access and lifecycle. This migration has NOT been applied remotely. It depends on existing knowledge/research migrations plus `indexed_sha256` from document ingestion.
- **Assistant connection:** `verified-calculations.ts` reads only authenticated server rules and supplies bounded, deterministic minimum qualifying mixed-level combinations to the assistant prompt. No request-body rule data is accepted. Zero autonomous contribution is explicit; eligibility and nonzero contribution are not inferred. Tests include 20+20+10 and authority-unavailable/new-season states. No real rule has yet been reviewed/activated, no paid end-to-end answer test occurred, and prompt inclusion alone cannot guarantee that the model's final wording is correct. The original live reasoning defect is NOT closed. Still needed: administrator review UI, verified official seed data, authoritative calculation display/answer validation and actual release acceptance.
- **C:** Browser-local validated workspaces now save routes, saved comparison candidates, camera settings and assumptions, partitioned by dimensions/motion profile and hopper capacity. Corrupt stored data is preserved rather than overwritten; unavailable/full storage has visible EN/HE feedback. This is browser-local, not account/cloud persistence. Draft JSON export still contains the selected draft only, not the saved candidate collection. Browser reload verified edited X=-5.5 and the comparison candidate before the capacity-key extension; final capacity-key workflow needs the subsequent browser check recorded below.
- **C:** Animated/scrubbable 2D route preview uses the same acceleration/braking, sequential turns and action timing as evaluation; invalid routes cannot play, changing a plan resets playback, and hidden-tab playback pauses. It does not actuate physical practice or robot code.
- **C/E:** Planned preload and per-action piece quantities now check capacity and shoot-before-pickup. Capacity comes from the simulator configuration. Intake success is assumed for inventory; success probabilities only affect assumed scoring. Official preload legality, stochastic inventory, real intake/shooter rates and parallel mechanism behaviour remain unvalidated.
- **E:** Bounded visibility-graph detours around inflated obstacle proxies and up to 200 intermediate waypoint orders (maximum 8 input waypoints, 60 obstacle boxes). Reordering is opt-in for independent actions; start/finish and action waypoints are retained. Invalid inventory/time/clearance candidates are excluded. Top 10 feasible candidates may be loaded into the editor. Tests verify wall detours, impossible starts, shorter waypoint orders, duration rejection and shoot-before-intake exclusion. This is not full game-aware strategy optimization: no partner reservations, official scoring constraints, camera-aware ranking or global optimum claim. Generated candidates are transient until loaded and saved.

Remaining independent implementation is substantial: complete reviewed season/rule administration and evaluated assistant output, actual CAD mesh camera occlusion/visual overlays, account-owned revisioned plans, official interaction constraints and alliance-aware route objectives. D still needs the actual offseason repository and command mapping; physical calibration requires workshop testing. Do not rebuild existing delivered controls or spend paid calls without a targeted acceptance case.

### Continuation checks

`test-season-authority.mjs` and expanded `test-season-planning.mjs` pass. Simulator fidelity regression passed (feeding, 504-ball conservation, opponent scoring, independent results, replay, official 32 tag poses and CAD integrity). Browser verified restored route/candidate, generated feasible alternative and advancing preview slider. Final TypeScript check and Vite build passed (existing large-chunk warnings remain). After the capacity-key extension, a real reload retained preload 3, X=-5.5 and Route 1; generation returned one feasible alternative. Physical device checks, responsive visual acceptance and paid-answer evaluation remain open. No production migration/deployment, paid call, budget change or APK build in this continuation.

**A — Reliable knowledge and reusable seasons.** Resolve the known mixed-scoring reasoning failure using deterministic computation, not prompt edits alone. Versioned structured official rules, citations/source hashes, units and season identity; generic discovery/retrieval with clear supported/unsupported states. Admin Check now prepares knowledge independently of field/simulation readiness. Review/activation, supersession/invalidation, immutable prior versions and rollback must be enforced server-side. Preserve the $25 cap and clarify daily spend versus remaining question count. Test EN/HE, multi-follow-up, new-topic/new-season replacement and numerical mixed-level cases.

**B — Camera placement/count advisor.** Use actual robot/field geometry in a common verified coordinate frame; user-editable mount candidates, lens/resolution, moving mechanism poses, permitted/forbidden mount areas and route samples. Show mounts/cones and candidate views on the model; tag front-face, projected size, distance and occlusion checks; coverage and camera-failure comparisons. Evaluate localization separately from game-piece/intake detection. Prefer the least complex camera set meeting the chosen coverage objective. No geometric result alone establishes localization precision or physical detection performance.

**C — Autonomous route editor.** Draw/edit waypoints/headings plus intake/shoot/wait actions; acceleration/speed/turn/clearance constraints; collision checking along segments; timing, action duration, game-piece accounting and driver-handoff pose/mechanism state. Shared season/robot revisions, visible assumptions, persistence/import/export, route preview/simulation, accessibility and EN/HE. Driver practice must not lose settings or be actuated when editing a plan.

**D — Repository-bound code generation.** Pin actual robot repository/commit, drivetrain/path-following library/version and command mappings. Generate reviewable path/configuration plus actual compatible code changes, validate builds/tests in isolation, link existing task/mentor review. No deployment-ready claim from a JSON route, pseudocode or mock interfaces. Repository remains unidentified until workshop, as previously recorded; do not repeatedly ask remotely. Continue independent work while this input is missing.

**E — Route recommendations.** Generate feasible candidate routines from legal start/goal/game-piece/alliance constraints and available time; rank expected achievable points/reliability, reserve margin and driver handoff, incorporating camera coverage. Account for action ordering, inventory, simultaneous/sequential behavior and partner reservations. Explain best among evaluated candidates, not global optimality. Measured robot data is required to validate predicted execution/performance.

## First implementation checkpoint — local only, NOT A–E complete

- `season-calculations.ts`: season-independent bounded enumeration of robot scoring combinations, including zero/no-score and optional aggregate autonomous contribution. Tests include 20+20+10, 30+20+0, failed 10+10+20, auto contribution and a synthetic different-season rule. This module is **not yet connected to G3 Assist or published official rules**; the live AI reasoning defect remains open.
- `seasonPackage.ts`: bounded import validation for identity/revision, metres/centered coordinate frame, source hashes, field boxes/tags, duration, scoring and supported interactions. Readiness helper is local configuration only, **not server authority**. No Check now/database activation connection yet.
- `autonomousPlanning.ts`: conservative straight-segment circle-envelope clearance; acceleration-limited stop-at-waypoint travel and sequential turn-time estimates; action assumptions; duration margin; final handoff pose; rank user-saved candidates. **Not a trajectory controller or autonomous candidate generator**. Manual points/probabilities are unverified and not official game scoring.
- `cameraPlanning.ts`: geometric tag-center FOV, face, projected-size approximation, field/robot box obstruction and candidate-set comparisons. Tests cover back-face, field occlusion, robot-panel occlusion, facing direction and avoiding extra cameras without additional coverage. **No CAD-mesh raycasting, game-piece detector, camera-loss optimization or physical calibration yet.** Current UI passes field proxies only.
- `EngineeringPlanner.tsx`: collapsible local draft workspace on existing field page, drawn/numeric waypoints and actions, time/clearance results, manual candidate comparison, camera candidate controls and JSON draft export/config import. Uses existing robot dimensions/speed/turn; acceleration/duration are explicitly unmeasured assumptions. Reference field uses existing 2026 assets/tags and simplified obstacles. It does not change gameplay or activate a season.
- Planner is kept mounted across fullscreen and memoized against unrelated driving state changes. Camera controls are collapsible. No backend migration, paid call, new APK or production deployment made in this checkpoint.

## Validation / remaining before release

Pure calculation regression suite `node scripts/test-season-planning.mjs` passed. Final TypeScript and Vite production build passed; existing large-bundle warnings remain. Browser opened real local field page, planner, typed X=-3.2 and saved a candidate; estimated time updated from 2.0 to 2.4 seconds. Desktop visual layout inspected. Latest cold load and Hebrew language switch passed. Fullscreen hid the planner and retained X=-3.2 after exit. Draft parser round-trip, invalid numbers/cameras and approval-spoof rejection passed. Physical mobile, responsive viewport, actual file-picker round-trip and broader error-state acceptance remain. Do not describe a tested mathematical module as a complete production capability.

Known first-draft limitations requiring further work: JSON draft export/import is implemented with robot-profile compatibility checking; automatic persistence is not implemented, so export before reloading/leaving. Saved candidate sets are in-memory only; imported fields are 2D configuration only; camera field boxes are crude proxies; readiness lacks database activation/ACL/version enforcement; duration/scoring are not connected to verified season rules; camera placement has no actual robot-CAD overlay; no route animation/controller execution; no generated robot code; candidate comparison is manual, not generated optimal routes. Protect existing simulator tests as integration deepens.

Next implementation order: finish/validate draft persistence and UI; connect server-reviewed season/rule versions and deterministic assistant scoring; add CAD geometry/coverage overlays; validated route simulation and candidate generation; repository adapter/code generation when actual repository/configuration is available. The whole A–E programme remains active; this checkpoint is not a reduced substitute for the requested outcomes.

Existing simulator regression checks also passed: test-twin-fidelity (feeding, 504-ball conservation, opponent scoring, independent results, replay, official tags and CAD integrity) and test-practice-venue (timed recording/results, replay, carpet/venue/storage behavior). Production and APK remain unchanged.
