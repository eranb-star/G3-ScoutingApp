// Synthetic, isolated component fixture. No Supabase client or network writes.
import{createServer}from'vite';import react from'@vitejs/plugin-react';import fs from'node:fs/promises';import os from'node:os';import path from'node:path';import{fileURLToPath}from'node:url';
const app=fileURLToPath(new URL('../',import.meta.url)),root=await fs.mkdtemp(path.join(os.tmpdir(),'g3-review-ui-')),src='/@fs/'+app.replaceAll('\\','/')+'src/';
await fs.writeFile(path.join(root,'index.html'),'<html><meta name="viewport" content="width=device-width,initial-scale=1"><div id="root"></div><script type="module" src="/entry.tsx"></script></html>');
await fs.writeFile(path.join(root,'entry.tsx'),`import React,{useState} from 'react';import{createRoot}from'react-dom/client';import StageReviewers from '${src}components/StageReviewers.tsx';import ProjectConfigurations from '${src}components/ProjectConfigurations.tsx';import{ReviewResults}from '${src}components/ReviewResults.tsx';import{ReviewRequirements,emptyRequirement}from '${src}components/ReviewRequirements.tsx';import '${src}index.css';import '${src}teamHub.css';const he=new URLSearchParams(location.search).has('he');document.documentElement.dir=he?'rtl':'ltr';function App(){const[items,setItems]=useState([emptyRequirement()]);return <main style={{maxWidth:new URLSearchParams(location.search).has('mobile')?358:820,margin:'20px auto',padding:16,background:'white'}}><h1>{he?'בדיקת אבן דרך':'Milestone review'}</h1><ReviewResults projectId="synthetic" submission={{id:'synthetic',requirements:[{id:'r1',requirement:he?'עמידות המנגנון בעומס':'Intake load capacity',acceptance:he?'עמידה בעומס 150 ניוטון למשך 30 שניות':'Withstand 150 N for 30 seconds without permanent deformation',method:'test'}],evidence_items:[{title:'Load test report',revision:'run-3',url:'https://example.com/test/3'}]}} canReview canWaive busy={false} onSave={async()=>true}/><ReviewRequirements items={items} onChange={setItems} disabled={false}/><ProjectConfigurations projectId="synthetic"/><StageReviewers taskId="synthetic" primary="mentor1" onSaved={async()=>{}}/></main>}createRoot(document.getElementById('root')!).render(<App/>);`);
const server=await createServer({configFile:false,root,plugins:[{name:'fixture',enforce:'pre',resolveId(id){if(id.endsWith('/lib/localization'))return '\0fixture';if(id.endsWith('/supabase'))return '\0storage';},load(id){if(id==='\0storage')return `const chain={select:()=>chain,eq:()=>chain,in:()=>chain,order:()=>Promise.resolve({data:[{id:'config1',name:'Robot A',revision:'v3',configuration_kind:'as_built',details:'Synthetic'}]}),then:(f)=>Promise.resolve({data:[{id:'mentor1',display_name:'QA Mentor'},{id:'mentor2',display_name:'QA Mechanical Mentor'}]}).then(f)};export const supabase={from:()=>chain,rpc:async()=>({data:[],error:null})};`;if(id==='\0fixture')return `export const useLocalization=()=>({pick:(en,he)=>new URLSearchParams(location.search).has('he')?he:en});`;}},react()],resolve:{dedupe:['react','react-dom'],alias:{react:path.join(app,'node_modules/react'),'react-dom':path.join(app,'node_modules/react-dom')}},server:{host:'127.0.0.1',port:4207,strictPort:true,fs:{allow:[app,root]}}});await server.listen();console.log('Synthetic review fixture http://127.0.0.1:4207/');
