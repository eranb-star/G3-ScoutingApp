import{readFileSync}from"node:fs";const read=p=>readFileSync(new URL(p,import.meta.url),"utf8"),quality=read("../src/pages/ScoutingQualityPage.tsx"),analysis=read("../src/pages/AnalysisPage.tsx"),picklist=read("../src/pages/PicklistPage.tsx"),css=read("../src/teamHub.css"),sql=read("../../../backend/supabase/competition_intelligence_phase6_20260905.sql");const checks=[["Quality uses responsive report cards",quality.includes("quality-report-grid")&&css.includes("@media(max-width:720px)")],["Quality resolves an active event before reporting evidence status",quality.includes('from("events").select("id")')&&quality.includes('localStorage.setItem("g3_event_id",active.data.id)')],["Structured pit evidence is formatted for people",quality.includes('typeof x==="object"?Object.entries')&&quality.includes('map(([k,v])=>`${pretty(k)} ${value(v)}`)')],["Conflicts retain a required reason",quality.includes("resolution_reason:reason.trim()")&&quality.includes("textarea autoFocus required")],["Pit evidence has leadership review",quality.includes("review_pit_scouting_report")&&sql.includes("current_team_role() not in('team_leader','mentor','admin')")],["Pit verification preserves audit details",sql.includes("review_note")&&sql.includes("reviewed_by")&&sql.includes("reviewed_at")],["Analysis separates pit from match evidence",analysis.includes("PIT REPORTED")&&analysis.includes("G3 VERIFIED")],["Analysis excludes duplicate match reports",analysis.includes('is_duplicate.eq.false,is_duplicate.is.null')],["Picklist exposes verified pit coverage",picklist.includes("PIT VERIFIED")&&picklist.includes('.eq("status","verified")')],["Mobile quality avoids compressed table columns",css.includes(".quality-report-grid dl>div{grid-template-columns:1fr}")]];for(const[name,ok]of checks)console.log(`${ok?"PASS":"FAIL"} ${name}`);if(checks.some(([,ok])=>!ok))process.exit(1);
