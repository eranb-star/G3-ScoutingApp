// Actual assistant components; synthetic authentication/answers, no paid calls.
import {createServer} from 'vite';import react from '@vitejs/plugin-react';import fs from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import {fileURLToPath} from 'node:url';
const app=fileURLToPath(new URL('../',import.meta.url)),root=await fs.mkdtemp(path.join(os.tmpdir(),'g3-software-mentor-')),src='/@fs/'+app.replaceAll('\\','/')+'src/';
await fs.writeFile(path.join(root,'index.html'),'<meta name="viewport" content="width=device-width,initial-scale=1"><div id="root"></div><script type="module" src="/entry.tsx"></script>');
await fs.writeFile(path.join(root,'entry.tsx'),`import React from 'react';import{createRoot}from'react-dom/client';import{BrowserRouter}from'react-router-dom';import Assistant from '${src}pages/FrcAssistantPage.tsx';import '${src}index.css';import '${src}teamHub.css';document.documentElement.dir=location.search.includes('he')?'rtl':'ltr';createRoot(document.getElementById('root')).render(<BrowserRouter><aside style={{padding:12,background:'#302138',color:'white'}}>LOCAL SOFTWARE MENTOR REVIEW · synthetic answer · no paid calls · <a href="/">English</a> · <a href="/?he">עברית</a></aside><Assistant/></BrowserRouter>);`);
const mock=`const he=location.search.includes('he'),revision='6d5694217412633b1055e5ff2c3ac9a7adf92f78';let context=null;export const useMemberAuth=()=>({profile:{id:'preview-admin',role:'admin',active:true}});export const useLocalization=()=>({language:he?'he':'en',pick:(en,hebrew)=>he?hebrew:en});export const supabase={rpc:async(name)=>({data:name==='project_review_context'?[{task_id:'task1',project_id:'project1',title:'Review shooter behavior',submission:null}]:true,error:null}),functions:{invoke:async(name,{body}={})=>{if(name==='github-repositories')return {data:{repositories:[{owner:'GlueGunAndGlitter',name:'6740_robot_2024',defaultBranch:'main',private:false}]}};if(body.action==='prepare-software')return {data:{revision,paths:['src/main/java/frc/robot/RobotContainer.java','src/main/java/frc/robot/subsystems/ShooterSubsystem.java','build.gradle']}};context=body.software;return {data:{conversationId:'example',softwareContext:context,answer:'Synthetic preview answer: review the shooter command and sensor feedback before changing gains. [C1:L1-L3]',citations:[{title:'ShooterSubsystem.java:1–3 · '+revision.slice(0,8),url:'https://github.com/GlueGunAndGlitter/6740_robot_2024/blob/'+revision+'/src/main/java/frc/robot/subsystems/ShooterSubsystem.java#L1-L3'}]}};}},from(table){const q=new Proxy({}, {get:(_,key)=>key==='then'?(done,fail)=>Promise.resolve({data:table==='ai_conversations'?[]:[],error:null}).then(done,fail):()=>q});return q;}};`;
const server=await createServer({configFile:false,root,define:{'import.meta.env.VITE_G3_ASSIST_EXECUTION_MODE':JSON.stringify('budgeted-text-v1')},plugins:[{name:'mentor-fixture',enforce:'pre',resolveId(id){if(/(?:^|\/)(?:localization|memberAuth|supabase)(?:\.tsx?)?$/.test(id))return '\0mentor-fixture';},load(id){if(id==='\0mentor-fixture')return mock;}},react()],resolve:{dedupe:['react','react-dom'],alias:{react:path.join(app,'node_modules/react'),'react-dom':path.join(app,'node_modules/react-dom'),'react-router-dom':path.join(app,'node_modules/react-router-dom')}},server:{host:'127.0.0.1',port:4240,strictPort:true,fs:{allow:[app,root]}}});await server.listen();console.log('http://127.0.0.1:4240/');
