// Local transport for the reviewed Edge bundle and additive migration. No keys.
import {build} from 'vite';import fs from 'node:fs/promises';import http from 'node:http';import path from 'node:path';
const root=path.resolve(process.env.RELEASE_ROOT??'../..'),out=path.join(root,'docs/staging/software-mentor.local');await fs.mkdir(out,{recursive:true});
await build({configFile:false,build:{target:'es2022',minify:false,outDir:out,emptyOutDir:false,lib:{entry:path.join(root,'backend/supabase/functions/frc-assistant/index.ts'),formats:['es'],fileName:()=> 'index.ts'},rollupOptions:{external:id=>id.startsWith('https://'),output:{inlineDynamicImports:true}}}});
http.createServer(async(req,res)=>{const file=req.url==='/handler'?path.join(out,'index.ts'):req.url==='/migration'?path.join(root,'backend/supabase/software_mentor_context_20260924.sql'):null;if(!file){res.writeHead(404);res.end();return;}const source=await fs.readFile(file,'utf8');res.setHeader('Content-Type','text/html; charset=utf-8');res.end('<h1>Software Mentor release transport</h1><textarea aria-label="Release source" style="width:95%;height:80vh">'+source.replaceAll('&','&amp;').replaceAll('<','&lt;')+'</textarea>');}).listen(4241,'127.0.0.1',()=>console.log('http://127.0.0.1:4241/handler'));
